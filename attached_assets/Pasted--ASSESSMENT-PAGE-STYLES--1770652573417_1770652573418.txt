/* ========================================
   ASSESSMENT PAGE STYLES
   ======================================== */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    /* Colors */
    --primary-navy: #1B2B3A;
    --premium-gold: #D4AF37;
    --light-gray: #F5F7FA;
    --text-gray: #4B5563;
    --text-light: #6B7280;
    --border-gray: #E5E7EB;
    --white: #FFFFFF;
    --success-green: #10B981;
    --warning-orange: #F59E0B;
    --error-red: #EF4444;
    
    /* Typography */
    --font-heading: 'Playfair Display', serif;
    --font-body: 'Inter', sans-serif;
    --font-accent: 'Montserrat', sans-serif;
}

body {
    font-family: var(--font-body);
    background: linear-gradient(135deg, #F5F7FA 0%, #E5E7EB 100%);
    min-height: 100vh;
    padding-bottom: 60px;
}

.container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 24px;
}

/* ========================================
   PROGRESS BAR
   ======================================== */

.progress-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: var(--border-gray);
    z-index: 1000;
}

.progress-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--premium-gold), #C19A2E);
    width: 0%;
    transition: width 0.3s ease;
}

/* ========================================
   SECTIONS
   ======================================== */

.section {
    display: none;
    animation: fadeIn 0.5s ease;
}

.section.active {
    display: block;
}

.section-content {
    background: var(--white);
    border-radius: 16px;
    padding: 48px;
    margin-top: 60px;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* ========================================
   TYPOGRAPHY
   ======================================== */

.main-title {
    font-family: var(--font-heading);
    font-size: 42px;
    color: var(--primary-navy);
    text-align: center;
    margin-bottom: 16px;
    line-height: 1.2;
}

.subtitle {
    font-size: 20px;
    color: var(--text-light);
    text-align: center;
    margin-bottom: 40px;
}

.section-header {
    margin-bottom: 32px;
}

.section-number {
    font-family: var(--font-accent);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--premium-gold);
    display: block;
    margin-bottom: 12px;
}

.section-title {
    font-family: var(--font-heading);
    font-size: 32px;
    color: var(--primary-navy);
    margin-bottom: 8px;
}

.section-subtitle {
    font-size: 16px;
    color: var(--text-light);
    margin-bottom: 32px;
}

/* ========================================
   INTRO SECTION
   ======================================== */

.intro-box {
    background: var(--light-gray);
    border-radius: 12px;
    padding: 32px;
    margin-bottom: 32px;
}

.advisor-intro {
    display: flex;
    gap: 16px;
    align-items: center;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid var(--border-gray);
}

.advisor-photo-small {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    border: 3px solid var(--premium-gold);
}

.advisor-name-small {
    font-weight: 600;
    color: var(--primary-navy);
    margin-bottom: 4px;
}

.advisor-title-small {
    font-size: 14px;
    color: var(--text-light);
}

.intro-text {
    font-size: 16px;
    color: var(--text-gray);
    margin-bottom: 20px;
    line-height: 1.6;
}

.assessment-areas {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-bottom: 20px;
}

.assessment-areas li {
    font-size: 16px;
    color: var(--primary-navy);
    font-weight: 500;
    padding: 12px;
    background: var(--white);
    border-radius: 8px;
}

.intro-note {
    font-size: 14px;
    color: var(--text-gray);
    font-style: italic;
}

.privacy-note {
    text-align: center;
    font-size: 12px;
    color: var(--text-light);
    margin-top: 16px;
}

/* ========================================
   FORMS
   ======================================== */

.assessment-form {
    max-width: 500px;
    margin: 0 auto;
}

.form-group {
    margin-bottom: 24px;
}

.form-group label {
    display: block;
    font-weight: 600;
    color: var(--primary-navy);
    margin-bottom: 8px;
    font-size: 14px;
}

.form-group input {
    width: 100%;
    height: 52px;
    border: 1px solid var(--border-gray);
    border-radius: 8px;
    padding: 16px;
    font-family: var(--font-body);
    font-size: 16px;
    transition: all 0.3s ease;
}

.form-group input:focus {
    outline: none;
    border-color: var(--premium-gold);
    box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.1);
}

/* ========================================
   QUESTIONS
   ======================================== */

.question-block {
    margin-bottom: 40px;
}

.question-number {
    font-family: var(--font-accent);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--premium-gold);
    margin-bottom: 8px;
}

.question-text {
    font-size: 18px;
    font-weight: 600;
    color: var(--primary-navy);
    margin-bottom: 20px;
    line-height: 1.5;
}

.options {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.option {
    display: flex;
    align-items: center;
    padding: 16px 20px;
    border: 2px solid var(--border-gray);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    background: var(--white);
}

.option:hover {
    border-color: var(--premium-gold);
    background: var(--light-gray);
}

.option input[type="radio"] {
    margin-right: 16px;
    width: 20px;
    height: 20px;
    cursor: pointer;
    accent-color: var(--premium-gold);
}

.option input[type="radio"]:checked + .option-text {
    color: var(--primary-navy);
    font-weight: 600;
}

.option-text {
    font-size: 16px;
    color: var(--text-gray);
    flex: 1;
}

.red-flag-option {
    border-color: var(--error-red);
    background: #FEF2F2;
}

.red-flag-badge {
    display: inline-block;
    font-size: 11px;
    font-weight: 700;
    color: var(--white);
    background: var(--error-red);
    padding: 4px 8px;
    border-radius: 4px;
    margin-left: 8px;
}

/* ========================================
   BUTTONS
   ======================================== */

.btn-primary, .btn-secondary {
    font-family: var(--font-accent);
    font-size: 16px;
    font-weight: 700;
    text-decoration: none;
    text-align: center;
    padding: 18px 48px;
    border-radius: 8px;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-block;
    width: 100%;
}

.btn-primary {
    background: linear-gradient(135deg, var(--premium-gold) 0%, #C19A2E 100%);
    color: var(--primary-navy);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.4);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(212, 175, 55, 0.6);
}

.btn-secondary {
    background: var(--light-gray);
    color: var(--primary-navy);
    border: 2px solid var(--border-gray);
}

.btn-secondary:hover {
    background: var(--border-gray);
}

.btn-large {
    font-size: 18px;
    padding: 20px 60px;
}

.button-group {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 16px;
    margin-top: 40px;
}

/* ========================================
   RESULTS SECTION
   ======================================== */

.results-content {
    max-width: 700px;
    margin: 60px auto;
}

.score-display {
    text-align: center;
    margin-bottom: 48px;
}

.results-title {
    font-family: var(--font-heading);
    font-size: 36px;
    color: var(--primary-navy);
    margin-bottom: 32px;
}

.score-circle {
    width: 200px;
    height: 200px;
    margin: 0 auto 24px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--premium-gold), #C19A2E);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: 0 8px 24px rgba(212, 175, 55, 0.3);
}

.score-number {
    font-family: var(--font-heading);
    font-size: 64px;
    font-weight: 700;
    color: var(--white);
    line-height: 1;
}

.score-label {
    font-size: 14px;
    color: var(--white);
    opacity: 0.9;
    margin-top: 4px;
}

.score-verdict {
    font-family: var(--font-heading);
    font-size: 28px;
    color: var(--primary-navy);
    margin-bottom: 16px;
}

.score-description {
    font-size: 18px;
    color: var(--text-gray);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
}

/* Red Flag Alert */
.red-flag-alert {
    background: #FEF2F2;
    border: 2px solid var(--error-red);
    border-radius: 12px;
    padding: 24px;
    margin-bottom: 32px;
}

.red-flag-alert h3 {
    color: var(--error-red);
    font-size: 20px;
    margin-bottom: 12px;
}

.red-flag-alert p {
    color: var(--text-gray);
    font-size: 16px;
    line-height: 1.6;
}

/* Score Breakdown */
.score-breakdown {
    margin-bottom: 48px;
}

.score-breakdown h3 {
    font-size: 24px;
    color: var(--primary-navy);
    margin-bottom: 24px;
}

.breakdown-item {
    margin-bottom: 24px;
}

.breakdown-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
}

.breakdown-title {
    font-weight: 600;
    color: var(--primary-navy);
    font-size: 16px;
}

.breakdown-score {
    font-weight: 700;
    color: var(--premium-gold);
    font-size: 16px;
}

.progress-bar-container {
    height: 8px;
    background: var(--border-gray);
    border-radius: 4px;
    overflow: hidden;
}

.progress-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--premium-gold), #C19A2E);
    transition: width 1s ease;
}

/* Next Steps */
.next-steps-box {
    background: var(--light-gray);
    border-radius: 12px;
    padding: 32px;
    margin-bottom: 48px;
}

.next-steps-box h3 {
    font-size: 24px;
    color: var(--primary-navy);
    margin-bottom: 20px;
}

.next-steps-box ul {
    list-style: none;
    padding-left: 0;
}

.next-steps-box li {
    font-size: 16px;
    color: var(--text-gray);
    margin-bottom: 12px;
    padding-left: 28px;
    position: relative;
    line-height: 1.6;
}

.next-steps-box li:before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--success-green);
    font-weight: 700;
    font-size: 18px;
}

/* Results CTA */
.results-cta {
    text-align: center;
    padding: 32px;
    background: linear-gradient(135deg, var(--primary-navy), #0F1922);
    border-radius: 12px;
    color: var(--white);
}

.results-cta h3 {
    color: var(--white);
    font-size: 28px;
    margin-bottom: 16px;
}

.results-cta p {
    color: #E5E7EB;
    font-size: 16px;
    margin-bottom: 24px;
}

.results-note {
    font-size: 14px !important;
    color: var(--text-light) !important;
    margin-top: 20px !important;
}

/* ========================================
   RESPONSIVE
   ======================================== */

@media (max-width: 768px) {
    .section-content {
        padding: 32px 24px;
    }
    
    .main-title {
        font-size: 32px;
    }
    
    .section-title {
        font-size: 24px;
    }
    
    .button-group {
        grid-template-columns: 1fr;
    }
    
    .btn-secondary {
        order: 2;
    }
    
    .btn-primary {
        order: 1;
    }
    
    .score-circle {
        width: 160px;
        height: 160px;
    }
    
    .score-number {
        font-size: 48px;
    }
}